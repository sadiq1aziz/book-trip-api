Book Trip API
A Spring Boot backend service built to evaluate Java platform threads vs virtual threads (Project Loom) under concurrent load using Apache JMeter.
This project focuses on concurrency behavior, throughput, and tail latency rather than business complexity.
